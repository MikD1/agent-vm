# ai-dev-vm project config. Uncomment the modules you need.
modules:
  - node # Node.js LTS + npm/pnpm/yarn
  # - dotnet    # .NET SDK (LTS)
  # - docker    # Docker CE
  - claude # Claude Code CLI (needs node)

resources:
  cpus: 2
  memory: 2GiB
