name: {{.Name}}

# https://mise-versions.jdx.dev
modules:
  - node: lts
  - claude
  - codex

resources:
  cpus: 4
  memory: 4GiB
  disk: 120GiB

files:
  # claude-settings.json: ~/.claude/settings.json

scripts:
  # - provision/postgres.sh

mounts:
  # - ~/projects
